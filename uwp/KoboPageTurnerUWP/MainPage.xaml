<Page
    x:Class="KoboPageTurnerUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KoboPageTurnerUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Page.Resources>

    <Grid
        x:Name="RootGrid"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        ManipulationMode="TranslateX"
        ManipulationCompleted="RootGrid_ManipulationCompleted">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Full-bleed tap zones — the whole left/right half of the screen
             turns a page, mirroring a dedicated page-turner remote. -->
        <Grid x:Name="PreviousZone" Grid.Column="0" Background="Transparent" Tapped="PreviousZone_Tapped" />
        <Grid x:Name="NextZone" Grid.Column="1" Background="Transparent" Tapped="NextZone_Tapped" />

        <Border Width="1" HorizontalAlignment="Center" Background="{ThemeResource SystemControlForegroundBaseLowBrush}" />

        <Ellipse
            x:Name="StatusDot"
            Width="12"
            Height="12"
            Fill="Gray"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,20,0,0" />

        <Border
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Margin="0,12,12,0"
            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
            CornerRadius="20"
            Padding="4">
            <StackPanel Orientation="Horizontal">
                <Button
                    x:Name="InfoButton"
                    Content="&#8505;"
                    Style="{StaticResource IconButtonStyle}"
                    Click="InfoButton_Click" />
                <Button
                    x:Name="RestartButton"
                    Content="&#9881;"
                    Style="{StaticResource IconButtonStyle}"
                    Margin="4,0,0,0"
                    Click="RestartButton_Click" />
            </StackPanel>
        </Border>

        <Border
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="0,0,0,24"
            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
            CornerRadius="16"
            Padding="16,8">
            <TextBlock Text="Tap or swipe to turn pages" FontSize="14" />
        </Border>
    </Grid>
</Page>
